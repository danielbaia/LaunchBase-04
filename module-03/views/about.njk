{% extends "layout.njk" %} {% block head %}

<title>Sobre - DANIEL AMARAL</title>

{%endblock%} {% block content %}
<div id="wrapper">
    <img src="{{ about[0].avatar }}" alt="">
    <h1>{{ about[0].name }}</h1>
    <h2>{{ about[0].role }}</h2>
    <p>{{ about[0].description }} </p>
</div>

<div id="social" class="links">
    {% for link in about[0].links %}
        <a href="{{ link.url }}" target="_blank">{{link.name}}</a>
    {% endfor %}

</div>

{% endblock %}